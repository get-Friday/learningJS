function multiplicaVetor(vetor, int) {
    this.int = int
    return this.resultado = vetor.map(v => v * int)
}

function maiorQueCinco(resultado) {
    return resultado.filter(n => n > 5)
}

const vetor = [1, 2, 3, 4, 5]
console.log(multiplicaVetor(vetor, 3))
console.log(maiorQueCinco(resultado))
